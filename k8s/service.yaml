apiVersion: v1
kind: Service
metadata:
  name: shopping-list
  labels:
    app: shopping-list
spec:
  type: ClusterIP
  selector:
    app: shopping-list
  ports:
    - name: http
      port: 80
      targetPort: 8080
